extends layout

block content
  h1 All Articles


  ul
    each article in articles
      li
        a(href='/article/'+article._id) #{article.title}
        |  #{article.date}

    else
      There are no articles
